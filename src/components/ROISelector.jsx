import React from "react";

const ROISelector = ({ roi, setRoi }) => {
  // TODO: Implement region-of-interest selection UI
  return (
    <div>
      <p>ROI Selector (Resizable Crop Box)</p>
      {/* ROI selection UI here */}
    </div>
  );
};

export default ROISelector;