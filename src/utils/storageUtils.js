// Utility functions for managing in-memory or IndexedDB storage

export function saveImageToClass(className, imageData) {
  // TODO: Save imageData to the specified class (folder/array)
}

export function getImagesByClass(className) {
  // TODO: Retrieve all images for a class
}

export function deleteImageFromClass(className, imageIndex) {
  // TODO: Delete image by index from class
}